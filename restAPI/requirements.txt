# Some dependencies are already installed by restAPI-package

# Dependencies used for local development
pick==2.2.0
PyYAML==6.0.1
rich==13.7.1

# Dependencies used by services
python-dotenv==1.0.1
Unidecode==1.3.8
